---
pipeline:

  push:
    image: plugins/gcr
    repo: "${DRONE_REPO_OWNER}/${DRONE_REPO_NAME}"
    tags:
      - "${DRONE_COMMIT:0:8}"
      - "${DRONE_BRANCH}"
      - "build-${DRONE_BUILD_NUMBER}"
    secrets: [google_credentials]

    when:
      branch: [master]
      event: [push]

